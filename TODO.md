# TODO

- [x] Update `bootstrap/app.php` to explicitly configure exceptions (`->withExceptions(...)`) so Artisan can resolve `ExceptionHandler`.
- [x] Re-run: `php artisan make:controller API/admin/DashboardController` to verify the fatal error is gone.


